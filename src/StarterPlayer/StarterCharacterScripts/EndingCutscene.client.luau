local i2 = 0 game.ReplicatedStorage.Cutscenes.EndingCutscene.OnClientEvent:Connect(function() workspace.CurrentCamera.CameraType = Enum.CameraType.Scriptable i2 = 0 for i, v in pairs(workspace.EndingCutscene:GetChildren()) do i2 = i2 + 1 end for i = 1, i2, 1 do task.wait(0) workspace.CurrentCamera.CFrame = workspace.EndingCutscene[i].CFrame end workspace.CurrentCamera.CameraType = Enum.CameraType.Custom end)